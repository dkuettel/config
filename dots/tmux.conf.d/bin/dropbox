#!/bin/zsh
set -eu -o pipefail

if which dbox &> /dev/null; then
	s=$(dbox status | head -n 1)
	if [[ $s == "Dropbox isn't running!" ]]; then
	elif [[ $s == 'Up to date' ]]; then
		echo '[✓] '
	else
		echo "[$s] "
	fi
fi
