#!/bin/zsh
set -eux -o pipefail

# setup or attach for ~/toys/drones

folder=~/toys/drones
name=${folder:t}

path=($folder/bin $path)

tmux new-session -s $name -A -c $folder -e h=$folder -e PATH=$PATH
