#!/bin/zsh
set -eu -o pipefail

cmd=(
    python -m
    nn.flow config
    # --stream=dkuettel/flow/test-stream-after
    --stream=dkuettel/flow/test-stream-before
    read-dataset
    --out-folder=.
)

run $cmd
