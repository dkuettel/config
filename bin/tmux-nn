#!/bin/zsh
set -eux -o pipefail

folder=${${1:-.}:a}
name=$(realpath --relative-base=$HOME $folder)

PATH=$folder/bin:$PATH

args=(
    new-session
        -s $name
        -c $folder
        -e h=$folder
        -e PATH=$PATH
        notes
    \;
    new-window -t :2 -n src
)
tmux $args
